# Remote MCP Bridge Configuration

# ============================================================================
# Bridge Server Configuration (for deploying the public bridge)
# ============================================================================

# Host and port for bridge server
BRIDGE_HOST=0.0.0.0
BRIDGE_PORT=8767

# Admin secret for managing tunnels
# IMPORTANT: Change this in production!
BRIDGE_SECRET=change-me-in-production

# Maximum idle time before closing tunnel (seconds)
MAX_TUNNEL_IDLE_SECONDS=300


# ============================================================================
# Tunnel Client Configuration (for local machine)
# ============================================================================

# Bridge server WebSocket URL
# For local testing: ws://localhost:8767
# For production: wss://your-bridge-domain.com
BRIDGE_URL=ws://localhost:8767

# Local MCP server URL (usually localhost)
LOCAL_MCP_URL=http://127.0.0.1:8766

# Local MCP API key (matches your MCP server config)
LOCAL_API_KEY=dev-key-local-only

# Your unique user identifier (generate with: python -c "import uuid; print(str(uuid.uuid4()))")
USER_ID=

# API key for external tools to access your remote endpoint
# Generate with: python -c "import secrets; print('lb_' + secrets.token_urlsafe(32))"
REMOTE_API_KEY=

# Comma-separated list of allowed tools
ALLOWED_TOOLS=search,search_agentic,open,summarize,list

# Keepalive interval (seconds)
KEEPALIVE_INTERVAL=30
