# MCP Bridge Server Configuration
# Deploy this to your remote server (146.190.120.44)

# Host and port
BRIDGE_HOST=0.0.0.0
BRIDGE_PORT=8767

# Admin secret (generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))")
BRIDGE_SECRET=your-admin-secret-here

# Tunnel idle timeout (seconds)
MAX_TUNNEL_IDLE_SECONDS=300
